from distutils.core import setup
import py2exe

setup(console = ['monitor.py'])

'''
change director to the project folder
and then type "python setup.py py2exe"
(DEFINE_SETTING should copy to the folder, which exe located)
'''
